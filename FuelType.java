// In Java, we can use the keyword "enum" to establish simple type that must take as its value.
// One of a set of predefined constant values.

public enum FuelType {
    STEAM, INTERNAL_COMBUSTION, ELECTRIC, OTHER; //fuel type can only be one of these four types, enum refers to these list of choices that you have
}
